{
  "name": "ongigil-app",
  "version": "0.1.0",
  "private": true,
  "description": "온기길 (Ongigil) — hands-free voice guidance PWA for elderly recyclable-paper collectors. No build step.",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "for f in js/*.js icons/*.js tests/*.js; do node --check \"$f\"; done",
    "serve": "python -m http.server 8150",
    "icons": "node icons/generate-icons.js"
  }
}
